[project]
name = "grogger"
version = "0.1.0"
description = "Grady Logger, a custom logger for python that works the way I like it"
readme = "README.md"
authors = [
    { name = "Josh Grady", email = "josh.g.codes@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
